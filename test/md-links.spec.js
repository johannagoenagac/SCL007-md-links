
global.window = global;
global.assert = require('chai').assert;
require('./module.js');

describe('mdLinks', () => {
    it('Debería ser una funcion', () => {

    });
});


describe('Leer un directorio', () => {
    //Así se testea algo con promesas
    test('Debería ser capaz de leer el directorio del programa', () => {
        //el número indica la cantidad de expects que tienes en tu test


    });
});


describe('leer un archivo', () => {
    it('debería ser un archivo .md', () => {

    })
})

describe('imrpimir links en un objeto', () => {
    it('objectLinnks debería ser un objeto', () => {

    })
})
